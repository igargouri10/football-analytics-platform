# models/staging/sources.yml - SNOWFLAKE VERSION

version: 2

sources:
  - name: football_data_raw
    description: "Raw football match data loaded into Snowflake"
    database: PROD
    schema: RAW
    tables:
      # This is the raw table we loaded the JSON into
      - name: RAW_MATCHES